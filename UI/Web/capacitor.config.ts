import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kavita',
  appName: 'kavita-mobile',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
